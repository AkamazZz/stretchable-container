Run the suite with `flutter test`.

Update the package goldens with `flutter test --update-goldens`.

These goldens are intended to be regenerated on the pinned local Flutter toolchain for this repo to avoid cross-version rendering drift.
